#+TITLE: RiftWarp — Project Tracker
#+STARTUP: overview

* Phase 1: Project Scaffolding [4/4]
** DONE Set up pnpm workspace root with package.json and tsconfig.base.json
** DONE Create packages/shared with types, constants, and protocol definitions
** DONE Create packages/client with Vite + React + Pixi.js scaffold
** DONE Create packages/server with Node.js + WebSocket scaffold
* Phase 2: WebSocket + Basic Game Loop [7/7]
** DONE Define message protocol (shared/src/protocol.ts) with discriminated unions
** DONE Implement server Ticker (fixed 20Hz loop with drift compensation)
** DONE Implement server ConnectionManager (track clients, buffer inputs)
** DONE Implement shared simulation tick function (shared/src/simulation.ts)
** DONE Implement client WebSocketClient (connect, send/receive, snapshot buffer)
** DONE Implement client InputManager (keyboard state, 20Hz send with sequence numbers)
** DONE Implement client rendering with Pixi.js (camera follow, arena grid, ship rendering)
* Phase 3: Ship Movement & Physics [5/5]
** DONE Implement core physics functions in shared (thrust, friction, movement, bounce, rotate)
** DONE Port all 8 ship stat arrays from g_fighterData to shared/src/shipData.ts
** DONE Port ship polygon vertex data from g_shipPoints to shared/src/shipGeometry.ts
** DONE Implement Ship entity with position, velocity, angle, health, shipType
** DONE Implement ShipRenderer (draw polygon via Pixi.Graphics + health bar + ship selection 1-8)
* Phase 4: Arena, Viewport, Stars, Wormholes [5/5]
** DONE Implement ViewportManager (camera follows local player)
** DONE Implement StarField (70 parallax stars, two layers)
** DONE Implement ArenaRenderer (boundary, grid lines)
** DONE Implement Portal entity (orbit center at distance=270, ARC_SPEED=0.5 deg/tick, animated rings)
** DONE Implement PointerRenderer (off-screen directional arrows to enemy wormholes)
* Phase 5: Combat System [5/5]
** DONE Implement collision detection (distance-based ship-bullet) in shared simulation
** DONE Port gun upgrade levels from g_shotData (4 levels with damage, size, numShots, maxBullets, fireDelay)
** DONE Implement Bullet entity (spawn at nose, inherit velocity + bullet speed, lifespan=100, wall bounce)
** DONE Implement CollisionSystem and DamageSystem (bullets hit enemy ships, damage, death)
** DONE Implement Explosion effects (expanding rings on ship death)
* Phase 6: Multiplayer Game Flow [4/4]
** DONE Implement GameRoom lifecycle (IDLE → COUNTDOWN → PLAYING → GAMEOVER with auto-restart)
** DONE Implement game session (track alive players, detect game-over, win counting)
** DONE Implement full world snapshot broadcast with player info (health, ship, wins, alive)
** DONE Implement PlayerSidebar component (name, ship type, health bar, wins, destroyed state)
* Phase 7: Powerup System [5/5]
** DONE Define all 20 powerup types in shared/src/powerupData.ts (stats, spawn ratios, names, colors)
** DONE Implement Powerup entity (bounce, lifespan=1200, invulnerable first 20 ticks)
** DONE Implement PowerupSpawnSystem (spawn at random portal after 40s, rate increases over time)
** DONE Implement PowerupCollectionSystem (self-use 0-5 apply immediately, sendable 6-19 to inventory max 5)
** DONE Implement PowerupFireSystem (Shift key fires powerup as orange projectile)
* Phase 8: Wormhole Enemy Types [15/15]
** DONE Implement enemy framework (14 types with stats, AI behaviors, spawn counts)
** DONE Implement HeatSeeker (12 per powerup, track player, rotate=16, thrust=7)
** DONE Implement Turret (1 per powerup, orbit wormhole)
** DONE Implement Mines (12 per powerup, static, explode on contact)
** DONE Implement UFO (3 per powerup, track player)
** DONE Implement Inflater (4 per powerup, track player)
** DONE Implement MineLayer (2 per powerup, wander)
** DONE Implement Gunship (1 per powerup, strong tracker)
** DONE Implement Scarab (2 per powerup, orbit wormhole)
** DONE Implement Nuke (1 per powerup, straight line, high damage)
** DONE Implement WallCrawler (1 per powerup, wander)
** DONE Implement SweepBeam (1 per powerup, orbit)
** DONE Implement EMP (1 per powerup, track player)
** DONE Implement GhostPud (1 per powerup, track player)
** DONE Implement Artillery (2 per powerup, wander)
** DONE Implement portal-bullet collision (powerup bullets hitting enemy portals spawn enemies)
* Phase 9: Ship Specials [5/5]
** DONE Implement Turtle special (E key destroys visible enemies in 300px range, costs 20 HP)
** DONE Implement Flash special (E key toggles between Squid and Tank forms, changes stats)
** DONE Implement Hunter special (E key fires 17 homing Piranha missiles, 3 charges, regen 20s)
** DONE Implement Flagship special (E key toggles attractor/repulser field)
** DONE Implement tracking cannon system (auto-target nearest enemy, lead calculation, Rabbit/Flagship)
* Phase 10: Lobby System [6/6]
** DONE Implement server TableManager (table CRUD, player slots, chat routing)
** DONE Implement LoginScreen (username entry, CONNECT button)
** DONE Implement LobbyScreen (player list, table list, + NEW TABLE button)
** DONE Implement auto-join on table creation, LEAVE button in game HUD
** DONE Implement lobby chat (type to chat, broadcast to all)
** DONE Rewire server index.ts for full lobby flow (login → lobby → table → game)
* Phase 11: HUD & UI Polish [0/3]
** TODO Implement GameHUD (status bar, powerup inventory, health, wins/kills)
** TODO Implement incoming powerup alert animation and nuke warning shake
** TODO Implement ShipSelector (ship selection grid between rounds with stats)
* Phase 12: Audio [0/2]
** TODO Implement AudioManager (Web Audio API, enable/disable toggle)
** TODO Add 5 sound effects (fire, thrust loop, explosion, powerup collect, silence)
* Phase 13: AI Bots [2/2]
** DONE Implement BotController (navigate, chase players, collect powerups, fight, fire, difficulty levels)
** DONE Implement auto-fill with bots (3 bots on table creation, named Ziggy/Nova/Blitz/etc.)
